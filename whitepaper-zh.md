内容网格协议白皮书 (v1.0)
概括
范围说明（当前实现）：网络目前仅聚焦于 Publisher 注册与 Verifier 驱动的徽章验证。下文涉及的矿工/任务分配/索引等内容属于历史设计或路线图，并未在当前代码中启用。
Content Grid 是一个去中心化的内容网络和搜索引擎协议，旨在构建一个更加公平、开放和内容驱动的互联网。我们采用创新的“有用的工作量证明”机制来激励网站发布者贡献高质量的内容，并激励网络节点提供爬行、计算和索引服务。

与依赖广告收入和不透明算法的传统搜索引擎不同，内容网格利用区块链技术、全节点向量索引和向量相似性搜索来创建由社区拥有和维护的抗审查内容发现引擎。该协议的原生代币CONGRID是整个经济生态系统的核心，用于质押、支付、奖励和治理，以确保所有参与者的利益与网络的长期健康发展一致。

1. 愿景与问题
1.1 现有互联网的困境
当前的互联网内容生态系统被少数中心化巨头垄断。这引出了几个核心问题：

算法黑匣子：内容的排名和可见度由不透明的商业算法决定，创作者很难获得公平的曝光。
审查和控制：中心化平台有权单方面删除内容或禁止帐户，威胁言论自由。
价值分配低效：广告驱动的模式让大部分价值被平台获取，内容创作者的收入受到严重挤压。
信息孤岛：内容被锁定在各个平台内，使得跨平台发现和连接变得困难。
1.2 我们的解决方案：去中心化的内容经济
内容网格旨在通过以下方式解决这些问题：

去中心化索引：建立不受任何单一实体控制的全球共享内容索引数据库。
基于内容的发现：通过先进的向量嵌入（Embedding）技术，实现基于内容语义相似度的搜索，而不是简单的关键词匹配。
公平的激励机制：奖励所有为网络做出贡献的参与者，包括内容发布者和网络节点运营者。
有用的工作：将传统区块链挖矿消耗的算力转化为爬取、分析、索引网页等有价值的工作。
2. 系统架构
Content Grid 采用分层架构，将链上共识与链下高性能计算解耦，实现可扩展性和效率。

（这是一个概念图占位符，实际图可以更详细地描述组件交互）

2.1 区块链层（协调层）
我们基于 Cosmos SDK 构建了一条名为 content-grid-chain 的主权应用链。这条链是整个系统的“大脑”和信任基础，负责：

身份和质押管理：节点运营者通过质押 CONGRID 代币来注册身份并获取网络权限。网站发布者需要注册并验证**一级域名**（主域名）的所有权。一级域名只能注册一次。
区块共识：采用标准Tendermint（CometBFT）BPoS共识机制。这是Cosmos SDK默认的解决方案，保证交易的即时终结性和较高的网络安全性。
任务分配：使用基于块哈希的确定性算法（Block Hash Based Assignment）。具体实现是`Hash(BlockHash + TaskID + Counter) % TotalMiners`，生成伪随机数种子，并确定性地从活跃矿工列表中选择执行节点。这种方法利用了 CometBFT 的不可预测性，而无需引入额外的 VRF 机制。
任务结果验证：采用基于链上共识的多数验证（On-Chain Majority Consensus）。矿工将结果哈希提交到链上，CometBFT 保证交易的顺序。链上的任务逻辑（当前范围已移除）会自动计算大部分提交的结果（需要 67% Quorum）。只有与多数结果一致的节点才能获得奖励。作恶或犯错误的节点将通过**Slashing**机制受到经济惩罚。
经济模型执行：负责CONGRID代币的奖励分配、交易费用处理和治理投票。

2.2 全节点索引层
与传统的分布式哈希表（DHT）分片存储不同，Content Grid的长期愿景是全节点索引模型：每个工作节点（矿工）维护整个网络的完整内容索引。

在实现方面，“向量数据库/ANN 索引”是可插入的（例如 Chroma、FAISS、HNSWlib 等）。为了确保效率和存储可扩展性，我们倾向于使用较短维的向量表示，并在需要多样性约束、重复数据删除和轻量级路由时进一步导出较短的相似性指纹/签名（例如128位/256位）。

2.3 链下执行与验证（Execution & Verification）
这是由独立工作节点运行的软件，负责执行所有计算密集型任务：

网页抓取和Embedding：节点抓取目标网页，计算向量embedding，并存储在本地索引中。
相似性查询服务：当发布者请求片段（包含推荐链接）时，网络根据当前块哈希随机选择一组矿工。这些矿工在本地查询与目标内容最相似的 10 个 URL 并返回结果。
验证机制：节点需要定期验证发布者。

在当前的代码实现中，还提供了一个轻量级的链下缓存服务`indexerd`：它自动从链上注册中心发现发布者，定期获取主页并计算嵌入，同时输出一个短签名。这样，验证者/其他组件就可以直接从`indexerd`获取缓存结果，避免重复访问和重复推理，同时保持大量嵌入链外。

3. 参与者的角色和激励措施
3.1 网站发布者（内容提供商）
参与方式：
注册其域名，系统将自动识别对应的**一级域名**（如`example.com`）并建立唯一的所有权，防止子域名被他人滥用。
在您网站的主页上嵌入包含协议验证信息（包括 `congrid.net` 链接和注册者钱包地址）的 HTML 片段。同时，该片段还将包含 10 个网络推荐的类似内容的链接。
获得激励：
可用性奖励：只要网站保持在线，并且代码片段中的链接经矿工验证与网络推荐结果高度重合（通过重合阈值检查），即可获得每日 CONGRID 代币奖励。
推荐流量：其网站链接会出现在其他内容类似的网站的HTML片段中，获得优质的推荐流量。
3.2 节点运营商（网络工人/矿工）
参与方式：
质押大量 CONGRID 代币成为替代工作节点。质押金额是其可信性和安全性的保证。
运行content-grid-d节点软件和本地向量索引服务来维护整个网络索引。
获得激励：
任务奖励：通过区块哈希算法选出并正确完成查询任务（返回10个最相似的URL）的矿工将获得奖励。
验证奖励：正确执行验证任务（检查发布者代码片段）的矿工也将获得奖励。
惩罚机制：如果链上共识仲裁判定任务没有正确完成（例如提交结果与多数不一致），节点将受到惩罚（Slashing）。
交易费用：作为链上的验证者，您收到打包交易产生的费用。
3.3 消费者（服务消费者）
参与方式：
任何需要内容发现、数据分析或 SEO 服务的个人或组织。
获取服务：
相似性搜索：免费或付费访问网络以进行高质量的内容相似性搜索。
发布赏金：支付 CONGRID 代币来发布自定义任务，例如：
反向链接购买：提供奖励以将您自己的链接添加到特定主题的网站。
按需抓取：付费让网络节点抓取并分析任何给定的网站。
4. 代币经济学
CONGRID是驱动协议运行的价值载体。以下是**目前的实施口径**以及后续计划。

4.1 代币效用（Utility）
- 质押（已实现）：验证者通过质押`x/verifiers`参与验证分配和收入。
- 支付（部分实现）：链上已经支持插槽/租赁场景的支付结算； “消费者赏金任务/高级API支付”仍在规划和完善中。
- 奖励（已实施）：发布者和验证者奖励在 `x/registry` 的回合最终确定中实施。
- 治理（部分实现）：链已具备治理模块的基本能力，但自定义模块参数治理入口仍在完善中。

4.2 供应和发行池（当前默认）
总创建参考量设置为10亿CONGRID（`ucongrid`为最小单位）。

当前默认发行参数：
- 运营商保留率：40%
- 发行池：60%（其中10%为发布者，50%为验证者）
- 释放期限：100年（按小时线性释放）

为了避免“每次将奖励金额逐一增加”，奖励改为池转移模式：
- 发行池的余额由代币经济模块维护；
- 每轮奖励都会从池子转移给接收者；
- 无人认领的部分将直接从池中销毁。

4.3 每轮（小时）分配及分配规则
在默认的一小时回合 (`round_interval_seconds=3600`) 下：
- 发布者池：~114.155251 CONGRID/小时
- 验证者池：~570.776255 CONGRID/小时

一般公式（每轮以秒为单位）是：
- __代码_0__
- __代码_0__

分配详情：
- 发布者：本轮活跃发布者优先平分；如果外部链接未达到阈值（`required_external_links_for_full_reward`），将按比例接收；无人认领的部分将被销毁。
- 验证人：仅分配给已通过并提交成功的验证人，权重与其承诺成正比，并叠加其邀请的活跃发布者因子（`stake × referral_factor`）；当无人认领时就被摧毁。

4.4 价值流动和通货紧缩（当前实施）
- 供给方：发布者/验证者奖励从既定的分配池中分轮释放。
- 通缩方面：无人认领的发布者奖励、不可用的验证者奖励和剩余余额将被销毁。

阐明：
- 文件中，“完整的区块级通胀路由、消费者奖励全链接、罚款没收全链接”等部分能力仍在分阶段落地；目前，以已推出的逻辑为准。

5. 路线图
第一阶段：协议核心实现
[✓] 完成内容网格链基础框架建设。
[✓] 实施节点质押和注册模块（x/节点、x/矿工）。
[✓] 实现网站注册和验证模块（x/registry）。
[✓] 基于区块哈希实现任务分配、共识和奖励模块（历史范围，当前已移除）。
第二阶段：链下功能和测试网上线
[ ] 开发链下任务执行器和全节点向量索引服务。
[ ] 发布内部测试网络，邀请早期节点运营商参与。
第三阶段：经济模型和消费功能上线
[ ] 启动激励测试网并引入发布者和代币奖励。
[ ] 开发API网关和消费者赏金任务功能。
第四阶段：主网上线及社区治理
[ ] 完成安全审核并正式上线主网。
[ ] 逐步将协议治理权转移给 CONGRID 代币持有者社区。
6. 结论
Content Grid不仅仅是一个区块链项目，更是一个构建更加开放、公平的下一代内容互联网的社会实验。通过将所有参与者的利益与网络的健康发展紧密结合起来，我们相信我们能够创建一个充满活力、自我进化、为所有用户带来真正价值的去中心化生态系统。我们邀请您与我们一起建设这个未来。
