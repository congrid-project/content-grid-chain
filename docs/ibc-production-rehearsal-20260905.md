# IBC 生产状态副本演练记录

生产快照取得于 2026-09-05 UTC，离线迁移与重开校验于 2026-09-06 UTC 完成。
本次结果：**生产状态数据库迁移回放通过**。主网没有执行 IBC 升级，未使用真实资金。

## 状态来源及一致性

- 链 ID：`congrid-main`。
- 运行版本：`publisher-rewards-v3`，源码提交
  `f913e38f5c2a5e422d777fcd947e33188768b230`。
- 快照高度：`70445`；该高度时间：`2026-09-05T21:30:17.6331913Z`。
- 原始应用哈希：
  `73B687F3E1B6C5E36FF6320579AE4CA1C36F3B2DF0FC6F8E89693ABA90DCD316`。
- 另外两台生产验证人在区块 `70446` 的头部记录了相同 `app_hash`。
  离线程序从加载的 IAVL 树重新计算哈希，结果也一致。
- 原始归档大小：`347649667` 字节；SHA-256：
  `6673b0d7f17b8a1167aef2df7e14ea154152ad6645a6ad7f5dc347e3fd4bb76c`。

来源是已同步、当前不参与共识投票的副本节点。检查其投票权重为 0，且公钥地址
不在当时的活跃验证人集合后，正常停止该节点并复制已关闭的数据库；约 33 秒后
恢复节点并确认追上主网。其余共识验证人没有停止。没有复制生产私钥或 keyring。
采集时临时使用的节点日志路径在收尾时通过日志轮换恢复，历史日志完整保留。

生产模块版本满足此次迁移的基线要求，包括 registry 3。线上普通治理投票期为
172800 秒，最低押金为 10000000 ucongrid；未来正式提案前仍应重新查询。

## 实际执行与核对

在原始归档解出的副本上，以生产提交构建旧版应用回放工具：

1. 加载高度 70445，核验状态哈希，导出原有模块状态和全部持久化存储摘要。
2. 仅在副本中注入 `ibc-transfer-v1` 计划，处理一个模拟空区块到 70446。
3. 旧版应用在模拟高度 70447 返回升级停止错误，并写出正常的 upgrade-info 文件。
4. 新版使用真实 store loader 打开同一数据库，新增 IBC 存储；所有旧存储摘要一致。
5. 执行真实升级 pre-blocker，核对迁移前后每个持久化键和值。
6. 新版提交模拟区块 70447–70449，退出后以另一进程重新打开数据库，核验状态和已执行计划。

迁移本身只改动以下三个存储：

| 存储 | 变化 |
| --- | --- |
| `ibc` | 新增客户端、连接参数及序号等默认状态 |
| `transfer` | 新增 transfer 端口及收发启用参数 |
| `upgrade` | 完成计划，记录 IBC 模块版本并推进协议版本 |

其余 15 个已存在存储全部逐字节一致，包括 `acc`、`bank`、`staking`、
`distribution`、`slashing`、`gov`、`registry`、`tokenomics` 和 `verifiers`。
其中包含 19 个账户、4 个质押验证人记录、3 个活跃验证人、1 个 Publisher，
以及 staking 的 10027 个持久化键和 registry 的 565 个持久化键。

原始 bank 总供应量为 `1000858515007025ucongrid`；迁移本身没有改变余额或供应量。
正常区块通胀及分配在迁移核对之后继续运行，不与迁移差异混在一起。
transfer 模块账户遵循 SDK 的按需创建行为，初始化阶段没有修改原账户或账户编号。

新增模块版本：`ibc=8`、`transfer=6`、`07-tendermint=0`。
ICS-20 端口为 `transfer`，`send_enabled` 和 `receive_enabled` 均为 true。
副本最终高度为 `70449`，重开后重新计算的应用哈希为：

```text
634593A84DF2514126BE70D2523509A97BA190B8D57DEEEFE21C7A6AB4E22CEA
```

## 测试范围

这是生产数据库副本的离线 ABCI 迁移回放，不是有真实共识签名的主网分叉。
升级计划在副本中直接安排，模拟提交票只使用公开验证人信息作为 ABCI 输入。
完整提案、签名投票、两个节点进程及 Hermes 跨链往返/退款流程已在独立本地链
通过，见 [IBC 测试说明](ibc-zh.md)。本次没有重做生产持币人的签名投票，
没有建立 Osmosis 主网通道，也没有投入 1000 USDC 流动性。

此次验证的是当前源码构建出的应用逻辑；正式发布还需要冻结发行提交、构建各平台
二进制并记录校验值，协调验证人的升级高度及切换安排。模拟高度 70447 仅属于
本次副本，不可用于新的主网提案。

## 复现和证据

- 工具：[构建脚本](../scripts/ibc/build_statecopy.py)、
  [回放程序](../scripts/ibc/statecopy/main.go)、[操作说明](../scripts/ibc/statecopy/README.md)。
- 本机证据目录：`tmp/ibc-production-20260905/`（不提交到 Git）。
- 原始数据：`snapshot.tar.gz`、`archive.json`、两个独立验证人的 `*-block-70446.json`。
- 数据库副本：`working-copy/`；报告为 `prepare-report.json`、`migrate-report.json`、
  `verify-report.json`，另有源状态导出、迁移前后存储摘要及全部变动键。
- 最初的工具预检漏用了节点现有的 `presenceDB` 适配层，导致空 IAVL 根重载报错。
  工具已改为复用所选源码中的 `cmd/content-grid-d/dbfix.go`；未修改生产数据库、
  依赖缓存或节点的该实现。修正后从相同副本完成上述验证。
